export enum Theme {
  Light = 'light',
  Dark = 'dark',
}

export enum Language {
  En = 'en',
  Es = 'es',
}

export enum Namespace {
  Common = 'common',
  Landing = 'landing',
  Account = 'account',
  Movement = 'movement',
}
