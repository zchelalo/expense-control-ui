export enum Auth {
  RefreshToken = 'refresh_token',
  AccessToken = 'access_token',
}

export enum SharedPages {
  Home = '/',
}

export enum PublicPages {
  Login = '/login',
  SignUp = '/sign-up',
}
